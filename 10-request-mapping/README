use xml config so solve a problem that occurs when the code is combined again, 
the server could not reload automatically. 
Annotation based can't solve but xml base can.
Now if i change something in my java file, it can compile automatically and reload the server.